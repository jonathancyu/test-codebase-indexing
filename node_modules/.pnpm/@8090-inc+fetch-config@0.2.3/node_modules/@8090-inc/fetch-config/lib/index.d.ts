#!/usr/bin/env node
import type { Config } from './config';
interface FetchConfigOptions {
    stack: string;
    outputDir?: string;
    profile?: string;
    region?: string;
    stackName?: string;
}
export declare function fetchConfig(options: FetchConfigOptions): Promise<Config>;
export {};
