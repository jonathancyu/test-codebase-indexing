# Data Layer

The data layer supports all components of the project including the API and frontend. The basis of the data layer is a PostgreSQL database that is hosted on AWS Aurora which contains the core `Users` table. This table contains a starting point for the project with a single table.

Information regarding the data model is found [here](data-model.md).

Information regarding connecting to the database is found [here](database-connectivity.md).
