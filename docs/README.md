# Documentation for PROJECT_NAME_PLACEHOLDER project

This is an mkdocs site for the PROJECT_NAME_PLACEHOLDER project.

How to run the site locally:

```bash
pip install -r docs/requirements-docs.txt
mkdocs serve -f docs/mkdocs.yml
```
